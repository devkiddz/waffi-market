# Shelsea Feed Experience Engine — Phase 1

This package contains actual TypeScript and TSX files. Copy `features/feed-experience` directly into the project root. No `src/` directory is used.

Audit order: contracts → selectors → builders → registry → engine → provider → modules → renderers → workspace.
